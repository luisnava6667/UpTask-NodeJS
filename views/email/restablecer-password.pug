doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        style
            include ./email.css
    body 
        h1 Restablecer contraseña
        p Hola #{nombre}
        p Hola, has solitado restablecer tu password, da click en el siguiente enlace para restablecerl, este enlace es temporal, asi que si se vence vuelve a solicitarlo
        a(href=`${resetUrl}`) Restablecer contraseña
        p si no puedes acceder a este enlace, visita #{resetUrl}
        p Si no has solicitado restablecer tu password, ignora este email